<%- include('partials/header') -%>
    <main class="container">
        <div class="row justify-content-center">
            <section class="col-6 mt-5">
                <% if (locals.messages.errors) { %>
                    <% messages.errors.forEach( el => { %>
                        <div class="alert alert-danger"><%= el.msg %></div>
                    <% }) %>    
                <% } %>
                <% if (locals.messages.info) { %>
                    <% messages.info.forEach( el => { %>
                        <div class="alert alert-info"><%= el.msg %></div>
                    <% }) %>    
                <% } %>
                <form action="/signup" method="POST">
                    <div class="mb-3">
                        <label for="userName" class="form-label aw-color">User Name</label>
                        <input type="text" class="form-control whiteBtn" id="userName" name="userName">
                      </div>
                    <div class="mb-3">
                      <label for="exampleInputEmail1" class="form-label aw-color">Email address</label>
                      <input type="email" class="form-control whiteBtn" id="exampleInputEmail1" aria-describedby="emailHelp" name="email">
                      <div id="emailHelp" class="form-text aw-color">We'll never share your email with anyone else.</div>
                    </div>
                    <div class="mb-3">
                      <label for="password" class="form-label aw-color">Password</label>
                      <input type="password" class="form-control whiteBtn" id="password" name="password">
                    </div>
                    <div class="mb-3">
                        <label for="confirmPassword" class="form-label aw-color">Confirm Password</label>
                        <input type="password" class="form-control whiteBtn" id="confirmPassword" name="confirmPassword">
                      </div>
                    <button type="submit" class="btn btn-primary whiteBtn">Submit</button>
                  </form>
            </section>
        </div>
    </main>

<%- include('partials/footer') -%>